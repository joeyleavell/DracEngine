#pragma once

float CalculateDiffuse(vec3 Normal, vec3 Incident)
{
	return dot(Normal, Incident);
}

float CalculateDiffuse(vec3 Normal, vec3 WorldPosition, vec3 LightPosition)
{
	vec3 Incident = normalize(WorldPosition - LightPosition);

	return dot(Normal, Incident);
}